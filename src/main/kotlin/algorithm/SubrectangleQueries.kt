package algorithm

class SubrectangleQueries(rectangle: Array<IntArray>) {
    val rect: Array<IntArray> = rectangle
    fun updateSubrectangle(row1: Int, col1: Int, row2: Int, col2: Int, newValue: Int) {
        (0..row1).forEach { i ->

        }
    }

    fun getValue(row: Int, col: Int): Int {
        return rect[row][col]
    }

}